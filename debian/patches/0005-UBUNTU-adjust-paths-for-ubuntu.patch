From: Tobias Heider <me@tobhe.de>
Date: Sun, 2 Feb 2025 12:40:47 +0100
Subject: UBUNTU: adjust paths for ubuntu

---
 update-m1n1 | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

diff --git a/update-m1n1 b/update-m1n1
index bf0c183..8705006 100755
--- a/update-m1n1
+++ b/update-m1n1
@@ -15,11 +15,11 @@ fi
 
 : ${M1N1:="/usr/lib/m1n1/m1n1.bin"}
 : ${TARGET:="$1"}
-: ${DTBS:=$(/bin/ls -d /usr/lib/linux-image-*asahi*/apple 2>/dev/null | sort -rV | head -1)/*.dtb}
+: ${DTBS:=$(/bin/ls -d /lib/firmware/*/device-tree/apple 2>/dev/null | sort -rV | head -1)/*.dtb}
 : ${CONFIG:=/etc/m1n1.conf}
 
-U_BOOT_DEF="/usr/lib/u-boot/apple_m1/u-boot-nodtb.bin"
-U_BOOT_ALT="/usr/lib/u-boot-asahi/u-boot-nodtb.bin"
+U_BOOT_DEF="/usr/lib/u-boot-asahi/u-boot-nodtb.bin"
+U_BOOT_ALT="/usr/lib/u-boot/apple_m1/u-boot-nodtb.bin"
 if [ -z "$U_BOOT" ]; then
     if [ -f "$U_BOOT_DEF" ]; then
         # Use Debian's u-boot as default unless otherwise specified
